{
  "name": "09-sqs-sns",
  "version": "1.0.0",
  "description": "Day 9 - SQS + SNS",
  "scripts": {
    "build": "tsc",
    "queue": "ts-node src/createQueue.ts",
    "topic": "ts-node src/createTopic.ts",
    "fanout": "ts-node src/fanoutPattern.ts",
    "dlq": "ts-node src/deadLetterQueue.ts",
    "cleanup": "ts-node src/cleanup.ts"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.0.0",
    "@aws-sdk/client-sns": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}