{
  "name": "11-cloudformation",
  "version": "1.0.0",
  "description": "Day 11 - CloudFormation",
  "scripts": {
    "build": "tsc",
    "deploy": "ts-node src/deployStack.ts",
    "update": "ts-node src/updateStack.ts",
    "drift": "ts-node src/detectDrift.ts",
    "cleanup": "ts-node src/cleanup.ts"
  },
  "dependencies": {
    "@aws-sdk/client-cloudformation": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}